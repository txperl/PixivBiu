node_modules/
dist/
out/

# Staged at build time by CI / `make desktop-dist` into per-arch dirs
# (resources/x64, resources/arm64) — never committed.
resources/**/pixivbiu
resources/**/pixivbiu.exe

# Provide real icons here; electron-builder auto-detects build/icon.{icns,ico,png}.
# (build/entitlements.mac.plist IS committed.)
!build/icon.ico
